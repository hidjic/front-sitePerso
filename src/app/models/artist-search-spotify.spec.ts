import { ArtistSearchSpotify } from './artist-search-spotify';

describe('ArtistSearchSpotify', () => {
  it('should create an instance', () => {
    expect(new ArtistSearchSpotify()).toBeTruthy();
  });
});
